SINTAXIS DEL COMANDO LET

let EXPRESION

EJEMPLOS

    #Definir variables
    a=10
    b=5
    c=2

    #Suma
    let sum=a+b

    #Resta
    let neg=a-b

    #Multiplicacion
    let mul=a*b

    #Division
    let div=a/b

    #Resto
    let resto=a%b

    #Exponenciacion
    let exponencial=a**b

    #Operaciones combinadas
    let resultado=(a + b) * c

    #Contador(INCREMENTAR)
    let contador++

    #Contador(DECREMENTAR)
    let contador--

    #Comparacion(MENORIGUAL)
    let menorIgual=a<=b

    #Comparacion(MAYORIGUAL)
    let mayorIgual=a>=b

    #Comparacion(MENORQUE)
    let menoQuer=a<b

    #Comparacion(MAYORQUE)
    let mayorQue=a>b

    #Compariacion(EQUIVALENCIA)
    let equivalencia=a==b

    #Compariacion(INEQUIVALENCIA)
    let inequivalencia=a!=b

    #Operacion logica(AND)
    let and=a&&b

    #Operacion logica(OR)
    let or=a||b

    #Operadores de asignacion compuestos
    =, *=, /=, %=,
    +=, -=, <<=, >>=,
    &=, ^=, |=	asignación

FORMA ABREVIADA DE let

    suma=$((a+b))